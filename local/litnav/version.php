<?php

defined('MOODLE_INTERNAL') || die();

$plugin->version   = 2012102400;      // If version == 0 then module will not be installed
$plugin->requires  = 2010031900;      // Requires this Moodle version
$plugin->cron = 0;
$plugin->component = 'local_litnav';
